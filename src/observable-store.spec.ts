import { ObservableStore } from './observable-store';

describe('Observable Store', () => {

  it('should be a placeholder and run', () => {
    const initialNumber = 1;

    expect(initialNumber).toBe(1);
  });
});
